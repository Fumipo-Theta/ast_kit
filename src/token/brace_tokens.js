import Token from "./token.js"

export class BraceStart extends Token {
    constructor(symbol) {
        super(symbol)
    }
}

export class BraceEnd extends Token {
    constructor(symbol) {
        super(symbol)
    }
}
